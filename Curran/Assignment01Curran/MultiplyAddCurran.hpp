/**
 * Performs a multiply-add operation (A*B + C) on three operands.
 *
 * @param a     the first factor in the multiplication
 * @param b     the second factor in the multiplication
 * @param c     the second term in the sum
 * @return      the quantity a*b+c
 */
double MultiplyAdd(double a, double b, double c);

