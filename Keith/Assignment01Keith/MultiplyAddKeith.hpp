/**
 * Computes and returns
 * \f[a * b + c\f]
 * 
 * @param a    the three input parameters
 * @param b
 * @param c
 * @return     $a * b + c$
 */
double MultiplyAdd(const double a, const double b, const double c);

