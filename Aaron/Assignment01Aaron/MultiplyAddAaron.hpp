    /**
     * This code will perform the multiply-add operation, R= A*B + C
     * 
     * @param A    the first number in the operation
     * @param B    the second number in the operation, to be multiplied by A
     * @param C    the third number in the operation, added to A*B
     * @return     the result R
     */
    double MultiplyAdd(const double A, const double B, const double C);


