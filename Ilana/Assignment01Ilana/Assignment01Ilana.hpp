/**
 * Performs a multiplication and addition operation, returning a*b + c
 * 
 * @param a     the first multiplied quantity
 * @param b     the second multiplied quantity
 * @param c     the added quantity
 * @return      the answer of a*b + c
 */
double MultiplyAdd(const double a, const double b, const double c);
