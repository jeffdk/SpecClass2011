//////////////////////////////////////////////////////////////////////////
/// Assignment02geoffrey
/// 
/// Here is defined a function that adds two vectors together, component
/// by component, and returns the sum divided by some double. 
/// Each component of each vector
/// should be a double.
///
/// @param A       first vector addend
/// @param B       second vector addend
/// @param divisor the double to divide the sum by
/// @return        (A + B) / divisor
///
//////////////////////////////////////////////////////////////////////////

MyVector<double> sumVectorsAndThenDivide(const MyVector<double>& A,
					 const MyVector<double>& B,
					 const double& divisor);

