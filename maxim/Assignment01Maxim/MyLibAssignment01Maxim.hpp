/**
 * Performs the “multiply-add” operation, returning a*b + c
 *
 * @param a	1st multiplier
 * @param b  	2nd multiplier
 * @param c	summand
 * @return	the capped quantity
 */
double MultiplyAdd(const double a, const double b, const double c);
