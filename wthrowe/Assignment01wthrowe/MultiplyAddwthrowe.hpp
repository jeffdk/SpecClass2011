/**
 * Multiplies two values together and then addss a third.
 *
 * @param a  a multiplicand
 * @param b  a multiplicand
 * @param c  a summand
 * @return   a * b + c
 */
double MultiplyAdd(const double a, const double b, const double c);
