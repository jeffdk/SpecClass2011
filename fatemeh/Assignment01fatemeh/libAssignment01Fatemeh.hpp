    /**
     * Multiply-Add: to multiply two doubles and add them by another double
     * @param a    = double
     * @param b    = double
     * @param c    = double 
     * @return     the capped quantity
     */
    double MultiplyAdd(double a, double b, double c);

